---
# defaults file for ansible-role-yum-repos

yum_repositories:
  - { n: epel-release }
  - { n: "http://rpms.famillecollet.com/enterprise/remi-release-{{ ansible_distribution_major_version }}.rpm" }
